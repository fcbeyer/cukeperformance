<%= form_for(@task_alert) do |f| %>
  <% if @task_alert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_alert.errors.count, "error") %> prohibited this task_alert from being saved:</h2>

      <ul>
      <% @task_alert.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :browser %><br />
    <%= f.text_field :browser %>
  </div>
  <div class="field">
    <%= f.label :time_limit %><br />
    <%= f.number_field :time_limit %>
  </div>
  <div class="field">
    <%= f.label :task_id %><br />
    <%= f.number_field :task_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
