<h1>Listing the features for <%= @current_suite.name %> from <%= @current_suite.build_date + "_" + @current_suite.build_time %></h1>

<table id="index">
  <tr>
    <th>Name</th>
    <th>Duration</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @features.each do |feature| %>
  <tr>
    <td><%= feature.name %></td>
    <td><%= feature.duration_converted %></td>
    <td><%= link_to 'Show', [@current_suite,feature] %></td>
    <td><%= link_to 'Edit', edit_suite_feature_path(@current_suite, feature) %></td>
    <td><%= link_to 'Destroy', [@current_suite,feature], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Feature', new_suite_feature_path(@current_suite) %>
<%= render 'suites/put_suites' %>
