<p id="notice"><%= notice %></p>

<table class="table table-bordered table-hover">
  <tr>
    <th>Name</th>
    <th>Duration</th>
    <th></th>
  </tr>
  <tr>
    <td><%= @current_feature.name %></td>
    <td><%= @current_feature.duration_converted %></td>
    <td><%= link_to 'Destroy', [@current_suite,@current_feature], confirm: 'Are you sure?', method: :delete %></td>
  </tr>
</table>

<%= render 'scenarios/put_scenarios' %>

<br />

<%= link_to 'Back', [@current_suite] %>
